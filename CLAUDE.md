# 个人知识库 · 任务说明（给 Cowork / Claude 读）

这是一个**个人知识库仓库**。用户（cimomi）会在对话里陆续发来知识素材——**金句、截图、文档、网页链接**。你的任务：把每一条素材**核查 + 精进 + 规范化**后，以统一 Markdown 格式入库，并自动重建带搜索/分类/跳转的 `index.html` 总览页，最后 commit + push。

> 模式参考：Andrej Karpathy 的 "LLM Wiki"（Markdown + frontmatter + 构建脚本 + 单文件 HTML 总览页）。

## 仓库结构

```
entries/        每条知识 = 一个 .md（带 YAML frontmatter）
assets/         图片/截图原件（按 entry id 命名，如 20260602-001.png）
build.py        扫描 entries/*.md -> manifest.json -> 注入 template.html -> index.html
template.html   总览页模板（设计已一次成型，含数据注入口 __KB_DATA__，勿改样式）
index.html      自动生成的总览页（勿手改）
manifest.json   自动生成的索引（勿手改）
```

## 每收到一条素材，执行以下流程

1. **识别类型**：金句 / 截图 / 文档 / 链接，其一。
2. **核查与精进**（价值核心，可多花算力）：
   - 链接 → 用 WebFetch/WebSearch 读原文，提炼要点、核实出处。
   - 金句 → 联网核查真实作者/出处，纠正常见误传，补背景与解读。
   - 截图 → 读图，提取文字与信息，必要时联网补全上下文。截图原件存入 `assets/`。
   - 文档 → 必要时转文本，提炼结构化要点。
   - **学术诚实**：不编造出处/数据，存疑就标注"出处待核"。
3. **写一个 `.md`** 到 `entries/`，文件名 `YYYY-MM-DD-<slug>.md`，严格按下方格式。
4. **重建索引**：在仓库根目录运行 `python3 build.py`（纯标准库，无需安装依赖）。
5. **提交**：`git add -A && git commit -m "kb: add <title>" && git push`。
6. 回复用户一句话：第几条、标题、归类。

## 知识条目统一格式

```markdown
---
id: 20260602-001          # 全局唯一，YYYYMMDD-序号
title: 精炼后的标题
date: 2026-06-02
type: 金句                 # 只能是：金句 | 截图 | 文档 | 链接
tags: [标签1, 标签2]        # 1-4 个
source: 原始出处或 URL      # 没有写"用户提供"
summary: 一句话简介（≤40字，显示在总览页卡片）
---

## 原始素材
> 用户发来的原文 / 截图引用 / 链接标题。截图用 ![](../assets/<id>.png)

## 精进与规范化
（联网核查后的结构化整理：要点、背景、解读、纠误——知识库的真正价值。）

## 延伸 / 关联
- 关联条目：[[其它条目id]]
- 进一步阅读：<url>
```

frontmatter 用最朴素的 YAML：字符串不加引号，列表写 `[a, b]`。`build.py` 用极简解析器，**勿用复杂嵌套**。

## 省 Token 铁律（务必遵守）

- **只处理当前这一条新素材**。绝不为"重建索引"去把已有 `entries/` 全部读进上下文——那是 `build.py` 在子进程干的活。
- 需要跨库查找/去重时，用 `grep -ril "关键词" entries/`，**不要批量 Read 整库**。
- `template.html` 已由 huashu-design 一次成型，**平时绝不重跑设计**；`build.py` 会自动把数据注入固定模板。仅当用户明确要求改版时才动它。
- 用户一次发来多条素材时：逐条写 `.md`，最后**只跑一次** `build.py` + 一次 commit。

## 关联与去重

写新条目前先 `grep` 标题/关键词查重；疑似重复就问用户合并还是新建。相关条目在正文用 `[[条目id]]` 互链。
